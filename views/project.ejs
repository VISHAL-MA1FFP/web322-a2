<!DOCTYPE html>
<html lang="en" data-theme="dim">
<head>
  <meta charset="UTF-8" />
  <link rel="stylesheet" href="/css/main.css" />
  <title><%= title %></title>
</head>
<body>
  <%- include('partials/navbar') %>

  <main class="container mx-auto p-8 prose max-w-4xl">
    <h1><%= project.title %></h1>
    <img src="<%= project.feature_img_url %>" alt="<%= project.title %>" class="rounded-lg shadow mb-4">
    <p><%= project.intro_short || project.summary_short %></p>
    <p><strong>Sector:</strong> <%= project.sectorName || project.sector %></p>
    <% if (project.original_source_url) { %>
      <p><a class="btn" href="<%= project.original_source_url %>" target="_blank" rel="noreferrer">Original Source</a></p>
    <% } %>
    <a href="/solutions/projects" class="btn mt-6">Back to Projects</a>
  </main>
</body>
</html>
